apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: process-companies-bucket
  labels:
    cloud.googleapis.com/location: us-central1
  annotations:
    run.googleapis.com/ingress: all
    run.googleapis.com/build-function-target: handle_gcs
    run.googleapis.com/build-base-image: us-central1-docker.pkg.dev/serverless-runtimes/google-22/runtimes/python311
    run.googleapis.com/build-enable-automatic-updates: "true"
spec:
  template:
    metadata:
      labels:
        run.googleapis.com/startupProbeType: Default
      annotations:
        autoscaling.knative.dev/maxScale: "10"
        run.googleapis.com/startup-cpu-boost: "true"
    spec:
      containerConcurrency: 80
      timeoutSeconds: 900
      serviceAccountName: your-service-account@your-project-id.iam.gserviceaccount.com
      containers:
      - name: process-companies-bucket-1
        image: us-central1-docker.pkg.dev/your-project-id/cloud-run-source-deploy/process-companies-bucket
        ports:
        - name: http1
          containerPort: 8080
        resources:
          limits:
            cpu: 2000m
            memory: 1Gi
        startupProbe:
          timeoutSeconds: 240
          periodSeconds: 240
          failureThreshold: 1
          tcpSocket:
            port: 8080
      runtimeClassName: run.googleapis.com/linux-base-image-update
  traffic:
  - percent: 100
    latestRevision: true
